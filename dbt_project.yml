name: nyc_analytics_project
version: '1.0'
config-version: 2

profile: nyc_analytics_profile  # <-- your profiles.yml profile

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  nyc_analytics:
    assignment_work:
      staging:
        +materialized: table