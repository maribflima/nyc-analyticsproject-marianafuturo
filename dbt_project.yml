name: nyc_analytics_project   # <-- REQUIRED
version: '1.0'
config-version: 2

profile: nyc_analytics_profile   # <-- set this to your dbt profile name

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  nyc_analytics:
    assignment_work:
      staging:
        +materialized: table